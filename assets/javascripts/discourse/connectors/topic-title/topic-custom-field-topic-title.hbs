{{#unless this.topic.editingTopic}}
  {{#if this.siteSettings.topic_priority_enabled}}
    {{#if this.isAllowedCategory}}
      <span>{{i18n
          "topic_priority.label"
          field=this.fieldName
          value=this.fieldValue
      }}</span>
    {{/if}}
  {{/if}}
{{/unless}}